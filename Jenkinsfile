pipeline {
    agent any

    stages {
       stage('Setting the variables values') {
    steps {
         sh '''#!/bin/bash
                 mv readme mehdimehrmanesh 
         '''
    }
}    
    }
}
